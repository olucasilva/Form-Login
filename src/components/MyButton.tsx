import { Button } from "@chakra-ui/react";

export const MyButton = () => {
  return (
    <Button
      colorScheme="teal"
      size="md"
      width="30%"
      alignSelf="center"
      type="submit"
    >
      Entrar
    </Button>
  );
};
